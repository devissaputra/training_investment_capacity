[project]
name = "training-investment-capacity"
version = "1.1.0"
description = "L&D research package on UK employer training reach, real investment intensity, and sector variation."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
